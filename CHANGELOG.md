# Changelog

> 版本变动说明。条目按版本倒序（最新在上），日期为该版本号引入提交的日期。
> 历史版本（v0.1.0 ~ v0.9.2）当时无 git tag，边界依据 `package.json` 的 `version` 字段首次出现的提交切分；此后新版本以 git tag 为准。

## [1.5.0] - 2026-08-27
### Added
- 新增网页型工具：编辑工具时类型可选「网页」，主区直接内嵌配置的网址（如本地管理台、监控面板），不再显示终端和文档面板；顶栏只保留「刷新」「在浏览器打开」和「编辑」。
- 网页切换不重载：切到别的工具再切回，网页保持原状态（登录、输入、滚动位置）；点「⟳ 刷新」按配置地址强制重载。网址不带协议时保存自动补全 `http://`。
- 站点拒绝被内嵌（显示空白）时，可用顶栏「↗ 浏览器」按钮在系统浏览器打开。
- 网页型工具不启动终端进程；把工具改成网页型会自动回收已启动的终端，改回默认型时原有终端配置完好恢复。

## [1.4.2] - 2026-08-25
### Fixed
- 修复执行命令后光标自动跳回终端最上方、新输入与屏幕已有内容重叠的问题。

## [1.4.1] - 2026-08-25
### Changed
- 配置记录弹窗改为左右双栏：左侧是记录列表，右侧显示所选记录的修改详情，新增行标绿、删除行标红，一眼看出每次改动的内容。

### Fixed
- 修复配置记录弹窗点击记录后看不到修改内容的问题（记录较多时尤其明显）。

## [1.4.0] - 2026-08-25
### Added
- 预览支持多开常驻：多个工具可各自打开网页或文档预览；切换到别的工具再切回，预览原样保留（网页不重新加载）。

### Changed
- 工具列表的收起按钮移到侧栏顶部「新建工具」按钮的左侧，打开编辑或预览时也能随时点击收起。
- 收起与展开状态的工具列表按钮位置、大小完全一致，切换时不再横向跳动。
- 编辑和预览窗口不再遮住顶部按钮行，只覆盖文档与终端区域；此期间终端与文档相关的顶栏按钮自动隐藏以防误点，工具列表的展开按钮始终可用。

## [1.3.1] - 2026-08-24
### Changed
- 数据存储位置从 `~/Library/Application Support/TermStep` 迁移到 `~/.config/TermStep`：首次启动自动完成搬迁并清理旧目录，工具与配置全部保留，无需手动操作。

## [1.3.0] - 2026-08-24
### Added
- 编辑器改为在工具界面内展开（不再是全屏弹窗），左侧工具列表保持可用：编辑时可随时切到其他工具查看或复制内容，再切回来草稿原样保留；可同时打开多个工具的编辑器，编辑中的工具在列表中带 ✏️ 标记。
- 关闭编辑器时若有未保存的修改会先弹确认，不再一键丢弃已输入的内容。
- 本地内容编辑区新增语法速查与快捷插入：点「? 语法」查看常用写法（每条可一键复制）；常用按钮属性（显示名、只粘贴不执行、色标等）和空命令块都可一键插入到光标处。

### Changed
- 编辑器表单改为对齐的网格布局，占用高度大幅缩小，本地内容编辑区明显变大；各字段的说明收进「?」悬停提示。
- 帮助文档的按钮语法说明改以 ` ### ` 结构化写法为主，旧简写降为兼容说明。
- 各弹窗底部的操作按钮统一为左对齐。

### Fixed
- 终端里前台程序被中断（如 `Ctrl+C`）退出后，输入显示异常的问题。

## [1.2.0] - 2026-08-05
### Added
- 命令按钮新增结构化写法：在命令后用 ` ### ` 分隔，可一次性设置显示名、是否只粘贴不执行、以及右端的彩色标签，例如 `deploy ### label=部署; edit; tag=高危; tag-color=red`；标签会以小色标形式显示在按钮右侧（红/黄/绿/蓝可选）。原来的 `命令 # 标签`、`命令 // edit` 简写仍可继续使用。

### Fixed
- 终端里单击或触控板轻点时不再频繁误进入文字选中状态（此前任何左键按下都会建立一个选区起点，导致轻微移动就出现突兀的高亮）；正常的拖拽选择、双击选词、`Shift`/`Alt` + 点击等操作不受影响。
- 帮助页的编辑弹窗现在只能通过按钮关闭，避免误点遮罩区域丢失正在编辑的内容。

## [1.1.2] - 2026-07-20
### Added
- 命令按钮支持「仅复制」模式：在命令后加 `copy` 标记，点击时只把命令复制到剪贴板、不执行，方便先确认或粘贴到别处再运行；这类按钮带剪贴板图标以示区别。

### Changed
- 终端浅色主题的配色调亮，改善白色背景下 tmux 分隔线、提示符等的可读性。

## [1.1.1] - 2026-07-20
### Changed
- 文档的编辑、删除、导出等操作按钮从文档面板上移到顶栏，与重启、显示/隐藏按钮排在同一行。
- 左右布局下文档面板改为固定宽度，缩放窗口时只改变终端宽度，文档区域不再被挤压。
- 顶栏的「终端」「文档」显示/隐藏按钮改为用背景色高亮表示当前可见状态，隐藏时低可见度；顶栏按钮按功能分组并用竖线分隔。
- 折叠文档时不再隐藏文档操作按钮，避免展开/折叠时的布局抖动。
- 快捷命令按钮改为普通样式，去掉原来的强调色。

## [1.1.0] - 2026-07-20
### Changed
- 布局系统重构：取消 1.0.0 引入的「终端型 / 仅文档型」两种模式，所有工具统一为「终端 + 文档」双面板布局。每个工具的终端和文档面板可单独显示或隐藏，状态按工具单独记忆；终端隐藏时点击命令按钮会提示「请先打开终端」（`⌘/Ctrl + 点击` 复制命令不受影响）。原有的「仅文档型」工具会自动迁移到新布局，默认隐藏终端。
- 顶栏新增「终端」「文档」两个显示/隐藏按钮，顺序调整为「重启 → 终端 → 文档」；文档的折叠按钮从帮助页内移到顶栏。

## [1.0.2] - 2026-07-20
### Added
- 带参数的命令按钮可为每个参数单独设置「显示名称」——参数键仍用于命令替换，表单上则显示更易读的中文名（如参数键 `keyword`、显示名「关键词」）。

### Fixed
- 终端里运行前台程序后点击「重启终端」会卡死的问题。
- 编辑工具时，输入的 `--`、直引号等会被系统自动替换成破折号、弯引号，导致命令和 JSON 失效；现在编辑框、参数表单等所有文本输入都禁用了这类自动替换。

## [1.0.1] - 2026-07-19
### Changed
- 文档型工具的文档区改为左侧章节导航 sidebar，章节列表与正文左右分栏、独立滚动，方便浏览和定位长文档。

## [1.0.0] - 2026-07-19
### Added
- **仅文档型工具**：工具现在可以是「终端型」或「仅文档型」两种模式。仅文档型工具不开终端，整个右侧合并为一个文档区，渲染 markdown 文档——给不用命令行的产品 / 运营角色当文档入口，查看 PRD、SOP、操作手册、外部系统链接。现有工具默认仍是终端型，完全兼容。
- 编辑工具时可选择模式（终端 / 仅文档）；文档里的链接照常走应用内预览，按钮仍可渲染但点击无动作（保留 `⌘/Ctrl + 点击` 复制命令）。

### Changed
- 1.0 里程碑：TermStep 从「命令按钮工具」扩展为「命令按钮 + 文档入口」双形态应用。

## [0.9.7] - 2026-07-19
### Changed
- 编辑工具改为弹窗形式，不再占用右侧栏，可与工具列表和帮助页同时查看。
- 编辑表单改为两列布局：分组、tmux 等短字段并排，工作目录等长字段占整行，整体更紧凑。
- 弹窗内的「保存」「取消」按钮固定在底部，长表单滚动时始终可见。

## [0.9.6] - 2026-07-19
### Added
- 可在命令按钮和快捷命令里用 `@/` 占位符引用工具主目录，点击执行时自动替换为实际路径。
- 编辑工具时能设置「工具根目录 (@/)」；未设置时 `@/` 依次回退到终端工作目录、用户主目录。

## [0.9.5] - 2026-07-18
### Added
- 工具可在分组之间拖拽移动：把工具拖到其他分组的标题或工具行上即可改变它所属分组，排序也会相应调整。
- 拖动工具时，落点指示更清晰：相邻工具之间只显示一个落点，拖到工具自身原位上下方不再出现多余的落点。

## [0.9.4] - 2026-07-18
### Added
- 工具分组：可在编辑工具时选择已有分组或新建分组，左侧工具列表按分组分区展示，分组可点击折叠/展开。
- 同一分组内可拖动工具调整顺序。
- 导入工具包时，重复导入同一份文件不再重复创建工具，而是更新已有工具。

### Fixed
- 删除或重排工具后分组标题偶发消失的问题。

## [0.9.3] - 2026-07-18
### Added
- 帮助文档里的网页链接和本地文档可以在弹层里直接打开预览，不必离开当前工具。
- 新增一键发布命令：自动同步版本号、提交、打版本 tag。

### Fixed
- 本地文档预览现在能正确识别 `~` 开头的路径（如 `~/Documents/readme.md`）。

## [0.9.2] - 2026-07-17
### Added
- 工具文档长内容分节折叠 + TOC 导航：帮助页长文档自动按 H2 分节折叠，配目录跳转。
### Changed
- 折叠 H2 去边框/padding，与未折叠文档样式一致，加大顶部留白。
### Fixed
- 工具保存内容无变化时跳过写入，避免无意义的版本控制提交。

## [0.9.1] - 2026-07-16
### Added
- 配置版本控制（git）：在 `configs/` 上做本地 git 快照，每次保存自动提交；每工具有独立配置记录历史。
### Changed
- 去掉工具详情工具栏按钮的图标，节省宽度。
### Fixed
- 深度安全审查修复：SSRF 防护 / 越权 / 竞态 / 注入防护。

## [0.9.0] - 2026-07-16
### Added
- 界面现代化重新设计：深浅主题切换 + 全新应用图标。

## [0.8.3] - 2026-07-14
- 版本号整理，无面向用户的新功能。

## [0.8.2] - 2026-07-14
### Added
- ⌘/Ctrl+点击命令按钮复制到剪贴板。
### Fixed
- `scan_tools` 不再读取 `useRemote:false` 的 mdUrl，修复 macOS「访问下载文件夹」弹窗。

## [0.8.1] - 2026-07-13
### Added
- 添加帮助按钮与应用内帮助文档。

## [0.8.0] - 2026-07-13
### Changed
- 快速添加改为任意 markdown 块原样追加。

## [0.7.3] - 2026-07-07
### Added
- 工具排序功能。
### Fixed
- 触控板手势下的拖拽排序与 peek 关闭问题。

## [0.7.2] - 2026-07-07
### Changed
- 使用 UUID 生成工具 ID（解决导入同名冲突）。

## [0.7.1] - 2026-07-07
### Fixed
- 路径穿越漏洞；增强 Mutex 容错处理（中毒锁跳过）。

## [0.7.0] - 2026-07-07
### Added
- 通用应用打包脚本（universal dmg）。

## [0.5.0] - 2026-07-07
### Changed
- 移除 Electron（src/main、src/preload、configs、依赖全部清理），Tauri 迁移完成。
### Fixed
- userData 路径 + 更新检查 TLS（迁移后修复）。
- 为所有 HTTP 请求加浏览器 User-Agent（Cloudflare 403）。
- 「检查更新」按钮无反应。

## [0.4.0] - 2026-07-06
### Added
- 浮动工具列表去掉管理按钮；支持读取本地 mdUrl 文件、右侧栏宽度可调。
- Tauri v2 后端从零搭建（4 个阶段）：
  - 阶段1：scaffold（Cargo.toml / tauri.conf.json / Vite 接线 / 图标源纳入 git）。
  - 阶段2：低风险模块（types/pure/updater/tools/cwd/tool_io/watcher/seed + 注册 24 命令 + 渲染端 IPC 层，34 个调用点改造）。
  - 阶段3：PTY 服务（portable-pty，6 个微妙行为 + generation guard 解决重启竞态）+ tmux 模块。
  - 阶段4：原生 macOS 菜单 + 检查更新菜单项。

## [0.3.0] - 2026-07-06
### Added
- 快速添加按钮、剪贴板 IPC、错误边界。
- 检查更新功能（全栈）：semver 比较 + manifest 解析 + 状态机（超时/防重入/去重）+ UpdateBadge 徽章/popover + sidebar 底部「检查更新」按钮。

## [0.2.0] - 2026-07-02
### Changed
- 正式改名为 TermStep。
- buttons 块支持 `// text` 作为纯文本标签。
- 优化折叠内容。

## [0.1.0] - 2026-06-30
### Added
- 核心 MVP（Electron 架构）：
  - 工具配置解析器（带默认值与校验）+ 工具目录扫描器。
  - buttons / buttons-json markdown 扩展：` ```buttons ` 围栏渲染可点击命令按钮。
  - buttons-json 参数化按钮：`{{name}}` 占位符弹表单，参数值 POSIX shell 转义，支持 hint/options/default/required。
  - 持久化 xterm 终端（每工具一个，懒加载 spawn + 输出流）。
  - 工具文件监听（ToolManager）+ IPC handlers / preload api / seed。
  - 侧栏工具列表（切换）+ 应用内编辑器（help markdown + tool 元数据）+ 新建/删除/排序。
  - 帮助 markdown 渲染 + 可点击命令按钮 + 快捷命令下拉。
- 应用打磨：自定义应用图标（终端提示符）+ `npm run icon` 脚本 + 原生应用菜单 + About 面板。
- 左右栏可折叠。
### Fixed
- 打包修复：登录 shell `-l` 启动（修复打包版 PATH 缺失）/ 未设 locale 回退 UTF-8（修复中文问号）/ 补全 TERM=xterm-256color、COLORTERM。

---

## 说明

- **没有 v0.6.0**：版本号直接从 0.5.0 跳到 0.7.0。
- **无 git tag**：版本边界完全依据 `package.json` 的 `version` 字段首次出现的提交切分，已逐个核对一致。
- 仅列用户可感知的功能/修复，略去纯文档与内部 chore 提交。

## 维护约定

- 每次升版本号（`npm run version:set <x.y.z>`）或打 git tag 时，脚本自动在顶部追加新版本条目（从 git log 提取并分类）；加 `--tag` 时同时执行 git commit + 打 tag。
- 版本号切分依据：以最近 git tag 为下界，无 tag 时回退到上一个版本号的引入提交。
